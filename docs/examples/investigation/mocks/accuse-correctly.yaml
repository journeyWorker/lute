# Trace mock (dsl 0.4.0 §4.3) — the detective accuses the RIGHT suspect.
# Drives scenes/confrontation.lute: seed the truth, force the accusation.
# `run.trueKiller` matches the forced `accuse` choice, so the success ending
# fires. NOTE: one deterministic mock-driven path, not proof of all paths.
state:
  run.trueKiller: blake
choose:
  accuse: accuseBlake

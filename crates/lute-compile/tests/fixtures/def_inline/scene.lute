---
kind: scene
# 0.21.1 T1-3/T1-4: component params substituted per expansion, a `{{@def}}`
# placeholder carrying its inlined body, and a constant def folded into a
# directive attribute.
character: demo
season: 1
episode: 1
components: [card.component.lute]
state:
  run.n: { type: number, default: 3 }
defs:
  twice: { type: number, cel: "run.n * 2" }
  closeUp: "1.3"
---

## One

::use{component="card" weather="grey"}
::use{component="card" weather="still"}
@narrator: Def {{@twice}}.
::camera{focus="demo" zoom=@closeUp}
